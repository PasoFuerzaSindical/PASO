export const metadata = {
  title: "PASO",
  description: "Sistema paso",
};

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  );
}
